name: "sdse-controlnet-finetune"
tag: "${rmspace:${system.prompt_processor.prompt},_}-step1"
exp: 'sd1.5_normalbae-finetune'
exp_root_dir: "outputs"
seed: 0
 
data_type: "random-camera-datamodule"
data:
  load_type: ${system.load_type}
  batch_size: 4
  eval_camera_distance: 4.0
  camera_distance_range: [1.5, 4.0]
  light_sample_strategy: "dreamfusion3dgs"
  height: 1024
  width: 1024
  eval_height: 1024
  eval_width: 1024

system_type: "gaussiandreamer-system"
system:
  load_type: 0 # 0: load from shape, 1: load from smpl
  load_path: "./load/shapes/stand.obj" # only used when load_type == 1
  radius: ${data.eval_camera_distance}
  sh_degree: 0
  guidance_eval_per: 500

  prompt_processor_type: "stable-diffusion-prompt-processor"
  prompt_processor:
    pretrained_model_name_or_path: "runwayml/stable-diffusion-v1-5"
    prompt: "a fox"
    negative_prompt: "oversaturated, smooth, pixelated, cartoon, foggy, hazy, blurry, bad structure, noisy, malformed"

  shape_prompt: ${system.prompt_processor.prompt}

  guidance_type: "stable-diffusion-controlnet-guidance"
  guidance:
    max_steps: ${trainer.max_steps}
    min_step_percent: 0.02
    max_step_percent: 0.98
    guidance_scale: 100
    condition_scale: 1.
    use_sdse: true
    use_sds: false
    grad_clip: [0,1.5,2.0,1000]
    fixed_size: 512
    tgt_with_cond: True
    # ControlNet finetuning settings
    finetune: true
    finetune_layer_name: "controlnet_down_blocks.11"
  
  # Reference the finetune layer name for optimizer configuration
  finetune_layer_name: ${system.guidance.finetune_layer_name}

  loggers:
    wandb:
      enable: false
      project: 'threestudio'
      name: ${name}_${tag}_${exp}

  loss:
    lambda_sds: 1.
    lambda_finetune: 1.
    lambda_sparsity: 1.
    lambda_opaque: 0.0

  optimizer:
    name: AdamW
    args:
      betas: [0.9, 0.99]
      eps: 1.e-15
    params:
      gaussian:
        lr: 0.001
      guidance.controlnet:
        lr: 0.0001

trainer:
  max_steps: 1800
  log_every_n_steps: 1
  num_sanity_val_steps: 0
  val_check_interval: 500
  enable_progress_bar: true
  precision: 16-mixed

checkpoint:
  save_last: true # save at each validation time
  save_top_k: -1
  every_n_train_steps: ${trainer.max_steps} 